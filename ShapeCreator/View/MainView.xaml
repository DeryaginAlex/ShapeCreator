<Window x:Class="ShapeCreator.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ShapeCreator"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Title="Shape Creator" Height="650" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="50*"/>
            <RowDefinition Height="50*"/>
        </Grid.RowDefinitions>

        <Menu Grid.Row="0">
            <MenuItem Header="Файл">
                <MenuItem Header="Создать новый проект" Command="{Binding CreateNewProjectCommand}"/>
                <MenuItem Header="Открыть" Command="{Binding OpenProjectCommand}"/>
                <MenuItem Header="Сохранить как" Command="{Binding SaveProjectCommand}"/>
            </MenuItem>
            <MenuItem Header="Логирование">
                <MenuItem Header="Сегодняшние логи"/>
                <MenuItem Header="Папка с логами"/>
            </MenuItem>
        </Menu>

        <ItemsControl 
            Grid.Row="1"
            ItemsSource="{Binding CanvasElements}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Canvas Background="Gainsboro"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>

        <DataGrid
            Grid.Row="2"
            x:Name="ShapesDataGrid"
            Margin="25"
            ItemsSource="{Binding Shapes, UpdateSourceTrigger=PropertyChanged}"
            SelectedItem="{Binding SelectedShape}"
            AutoGenerateColumns="False"
            CanUserDeleteRows="True"
            CanUserResizeRows="False"
            IsReadOnly="False"
            GridLinesVisibility="Horizontal"
            Padding="5"
            VerticalScrollBarVisibility="Visible">
            <DataGrid.Columns>
                <DataGridTextColumn
                    Header="Name" 
                    Binding="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                <DataGridTemplateColumn Header="Shape Type">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding ShapeType}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>

                    <DataGridTemplateColumn.CellEditingTemplate>
                        <DataTemplate>
                            <ComboBox
                                ItemsSource="{Binding DataContext.ShapeTypes, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                SelectedItem="{Binding ShapeType, Mode=TwoWay}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellEditingTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn 
                    Header="Start X" 
                    Binding="{Binding CoordinateStart.X, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                <DataGridTextColumn
                    Header="Start Y" 
                    Binding="{Binding CoordinateStart.Y, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    <DataGridTextColumn 
                    Header="End X" 
                    Binding="{Binding CoordinateFinish.X, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    <DataGridTextColumn 
                    Header="End Y" 
                    Binding="{Binding CoordinateFinish.Y, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    <DataGridCheckBoxColumn
                    Header="Active" 
                    Binding="{Binding IsActive, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</Window>
